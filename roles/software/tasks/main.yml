---
- name: Install required packages
  yum:
    name:
      - vim
      - git
      - nodejs
    state: present

- name: Install required NPM packages
  npm:
    name: '{{ item }}'
    global: yes
  loop:
    - axios
    - htmlparser2
